// Dark theme layout overrides
// Only define properties that differ from base theme
export const darkLayout = {
  // Dark theme can use default base layout values
  // Add overrides here if needed in the future
} as const;

export type DarkLayout = typeof darkLayout;
